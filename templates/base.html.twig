<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Symfothèque - Bibliothèque en ligne</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        main {
            flex: 1;
        }
    </style>
</head>

<body class="bg-gray-100 text-gray-800">

    <!-- Header Section -->
    <header class="bg-gray-600 text-white py-4">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-3xl font-bold">Symfothèque</h1>
            <nav>
                <ul class="flex space-x-4">
                    <li><a href="{{ path('app_book_index') }}" class="hover:underline">Livres</a></li>
                    <li><a href="{{ path('app_book_new') }}" class="hover:underline">Ajouter un livre</a></li>
                    <li><a href="{{ path('app_category_index') }}" class="hover:underline">Categories</a></li>
                    <li><a href="{{ path('app_author_index') }}" class="hover:underline">Auteur</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <!-- Main Content Section -->
    <main class="container mx-auto py-10">
        <div class="text-center mb-10">
            <h2 class="text-4xl font-bold mb-4">Bienvenue à Symfothèque</h2>
            <p class="text-lg">Votre bibliothèque en ligne pour découvrir et emprunter des livres.</p>
        </div>
        {% block body %}
        {% endblock %}
    </main>

    <!-- Footer Section -->
    <footer class="bg-gray-600 text-white py-4">
        <div class="container mx-auto text-center">
            <p>&copy; 2024 Symfothèque. Tous droits réservés.</p>
        </div>
    </footer>
</body>

</html>
